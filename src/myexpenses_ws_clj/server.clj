(ns myexpenses-ws-clj.server
  (:require [monger.core :as mg]))

; (let [conn (mg/connect)
;       db   (mg/get-db conn "my-expenses-debug")
;       u    (.toCharArray "jonathanrz")
;       p    (.toCharArray "ZaqLgME79HTAAR")]
    ;(mg/authenticate db u p)
; )
